# trading_platform/requirements-gui.txt
# Dependencies for the Desktop GUI Dashboard

PyQt6~=6.7.0  # Or PySide6~=6.7.0
# PyQt6-sip # Often installed automatically with PyQt6
# PyQt6-Qt6 # Core Qt libraries, usually a dependency


matplotlib>=3.7.0 # If you want to embed matplotlib plots in Qt

# python-dotenv~=1.0.1 # If the GUI's data_loader needs to load a .env for paths

# Core Data Handling & Numerics
numpy~=1.26.0
pandas~=2.2.0
backtrader~=1.9.78.123  # Latest stable version from PyPI

# Date/Time Utilities (often dependencies of pandas)
python-dateutil~=2.9.0
pytz~=2024.1
tzdata~=2024.1

tensorflow~=2.17.0    # For LSTMs and Keras API
scikit-learn~=1.5.0  # For StandardScaler and other utilities
joblib~=1.3.0           # For saving/loading scikit-learn objects like scalers

# Data Fetching & APIs
requests~=2.31.0        # HTTP library, often a dependency (e.g., for yfinance)
alpaca-py~=0.17.0        # Alpaca trading API (from your list)
# trading_platform/requirements.txt
# ... other packages ...
psycopg[binary]~=3.1.8  # Or your desired psycopg v3 version
psycopg_pool~=3.1.6   # Or your desired psycopg-pool v3 version
# ... other packages ...
PyYAML~=6.0.1
# Plotting
dash~=2.18.2  # Latest stable Dash version
plotly~=5.24.1  # Latest stable Plotly version
matplotlib~=3.8.0  # For plotting graphs
# Database
psycopg 

six~=1.16.0
python-dotenv~=1.0.0    # For environment variables (from your list)
retry~=0.9.2            # For retrying operations (from your list)
schedule~=1.2.0         # Job scheduling (from your list)
python-dotenv
